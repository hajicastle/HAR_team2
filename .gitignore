# Build output (로컬 빌드 시)
Debug/
Release/
build/

# OS / Python generated files
.DS_Store
__pycache__/
*.pyc

# Claude Code
.claude/

# clangd (LSP 인덱스 캐시 & 설정)
.cache/
.clangd

# 문서 폴더 (바이너리/대용량 파일 — git-lfs 미사용 시 로컬 관리)
docs/
